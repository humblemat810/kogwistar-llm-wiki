"""Entity-disambiguation contracts, service, and reconciliation helpers.

The service is intentionally imported from ``disambiguation.service`` rather
than eagerly from this initializer to avoid a cycle with the root contract
façade.
"""
