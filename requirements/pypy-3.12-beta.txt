# Deliberately bounded PyPy 3.12 beta profile.
#
# This file is used after the four local packages are installed with --no-deps.
# Keep NumPy, Chroma, pgvector, Torch, pikepdf, and GPU extras out of this
# profile. Resolve these constraints on the target interpreter in the manual
# workflow; do not use the full CPython/dev extra.

# Kogwistar base runtime
pydantic>=2.6
pydantic-extension>=0.0.7
anyio>=4.0
Jinja2>=3.1
fastapi>=0.111
mcp>=1.27,<2
httpx>=0.28.1
python-jose[cryptography]>=3.3
PyJWT>=2.8
RapidFuzz>=3.13.0
joblib>=1.3
python-dotenv>=1.0.1
requests>=2.32
typing-extensions>=4.8

# Parser text/workflow path. pikepdf is intentionally omitted; its import is
# lazy and the parser gives an actionable capability error for that path.
pdf2image>=1.17
pypdf>=6.5
pathspec>=0.12
langchain-core>=1.2
# Provider adapters are intentionally excluded from this provider-free lane.
# Their imports are lazy; live/provider contract tests remain CPython/manual.

# LLM-Wiki control plane and provider-free tests.
pytest>=8
pytest-anyio
sqlalchemy>=2.0
psycopg>=3.1
