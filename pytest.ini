[pytest]
testpaths = tests
pythonpath = src
norecursedirs = kogwistar-llm-wiki-tests-*
markers =
    manual: tests that are opt-in and may require local services
    ci_full: integration tests that require live parser/model services
    integration: tests that require real filesystem or external backends (no external service needed)
